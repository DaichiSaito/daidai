<%= link_to follow_path(current_user.follows.find_by(board_id: board.id)),
            method: :delete,
            id: "btn__unfollow-#{board.id}",
            class: 'btn__unfollow',
            style: 'font-size: 16px',
            remote: true do %>
  <%= fa_icon 'star' %>
<% end %>
